{
  "license": "MIT",
  "scripts": {
    "check-format": "prettier --list-different 'lessons/**/*.js'",
    "format": "prettier --write 'lessons/**/*.js'",
    "lint": "eslint 'lessons/**/*.js'",
    "test": "tap --no-cov",
    "test:cov": "tap",
    "test:cov:html": "tap --coverage-report=html",
    "test:watch": "tap -n -w --no-coverage-report"
  },
  "devDependencies": {
    "eslint": "^7.29.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.4.0",
    "mock-knex": "^0.4.10",
    "mock-require": "^3.0.3",
    "nock": "^13.1.0",
    "prettier": "^2.3.2",
    "tap": "^15.0.9"
  },
  "dependencies": {
    "fastify": "^3.18.0",
    "knex": "^0.95.6",
    "mysql2": "^2.2.5"
  }
}
